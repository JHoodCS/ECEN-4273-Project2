# This file will contain any custom layers for use in the network.
# This can include custom functions that represent "blocks" in the network where
# each block can contain multiple layers
# This will make building the network more like lego and less typey typey